# Call another pipeline. Run it within this build to capture the log output here. 
# A test for running a DevOps pipeline within a GitHub Check Run workflow.
pool:
    vmImage: 'windows-latest'

stages:
- stage: Stage 1
  jobs:
    - job: Job 1

    extends:
      template: -experimentalDummy.yml